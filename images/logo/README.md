# Logo

Place your logo here.

Recommended:
- **Format:** SVG (best) or PNG with transparent background
- **Minimum size:** 400px wide
- **Name the file:** `logo.png` or `logo.svg`

Once you drop the file in, the navbar automatically uses it. The text fallback hides as long as the image loads.
